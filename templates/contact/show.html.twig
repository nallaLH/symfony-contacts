{% extends 'base.html.twig' %}

{% block title %} {{ contactId.lastname }} {{ contactId.firstname }} {% endblock %}

{% block body %}

    <h1>{{ contactId.lastname }} {{ contactId.firstname }}</h1>
    <dl>
        <dt>Nom</dt>
        <dd>{{ contactId.lastname }}</dd>
        <dt>Prénom</dt>
        <dd>{{ contactId.firstname }}</dd>
        <dt>Email</dt>
        <dd>{{ contactId.email }}</dd>
    </dl>

{% endblock %}



